import { CalendarIcon } from '../icons/Icons';

export const ButtonCalendar = () => {
  return (
    <button
      className="header__button-theme"
      id="theme-button"
    >
      <CalendarIcon />
    </button>
  );
}